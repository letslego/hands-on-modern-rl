# E.4.2  KL 

> ****：[E.4.1 、](./information-basics)——。

---

。： vs ， vs 。—— KL 。

## ：""

""——。 $P$  $Q$ ，。

。，；，。""。：

$$
H(P,Q)=-\sum_x P(x)\log Q(x).
$$

， $\log$  $Q$  $P$——，""。

。：

$$
P=[1,0].
$$

：

$$
Q=[0.8,0.2].
$$

 $P=[1,0]$，：

$$
H(P,Q)=-\log 0.8.
$$

， $Q=[0.95,0.05]$， $-\log 0.95$， $-\log 0.8$ 。，——。 RLHF ，、。

---

## KL ：""

""。，""—— KL 。KL ，PPO  RLHF 。

，KL ： $P$， $Q$ ，""。：

$$
D_{KL}(P\|Q)=\sum_x P(x)\log\frac{P(x)}{Q(x)}.
$$

$\frac{P(x)}{Q(x)}$ —— $P$  $Q$  $x$ ， 1，$\log 1=0$，。， 1，KL 。

 RL 。PPO  RLHF 。：

$$
\pi_{old}=[0.5,0.5].
$$

：

$$
\pi_{new}^{(1)}=[0.6,0.4], \qquad \pi_{new}^{(2)}=[0.9,0.1].
$$

， 2 。 KL ： $P$， 1  $Q$：

$$
D_{KL}(\pi_{old}\|\pi_{new}^{(1)})
=0.5\log\frac{0.5}{0.6}+0.5\log\frac{0.5}{0.4}.
$$

 2  $Q$：

$$
D_{KL}(\pi_{old}\|\pi_{new}^{(2)})
=0.5\log\frac{0.5}{0.9}+0.5\log\frac{0.5}{0.1}.
$$

， 2 。

---

## KL 

 KL ，：KL 。 $D_{KL}(P\|Q)\neq D_{KL}(Q\|P)$。——PPO  RLHF ，。

$$
D_{KL}(P\|Q)\neq D_{KL}(Q\|P).
$$

，：

$$
P=[0.99,0.01], \qquad Q=[0.5,0.5].
$$

 $P$  $Q$（ $D_{KL}(P\|Q)$）：， $Q$ 。""，。

 $Q$  $P$（ $D_{KL}(Q\|P)$）：， $P$ 。""，。

 KL ，。PPO  $D_{KL}(\pi_{old}\|\pi_{new})$，""；RLHF  $D_{KL}(\pi_\theta\|\pi_{ref})$，""。。

---

## 

：

|       |                  |                                         | RL                   |
| --------- | ---------------------------- | ----------------------------------------------- | ---------------------------- |
|     |  | $H(P,Q)=-\sum_x P(x)\log Q(x)$                  | 、 |
| KL    |            | $D_{KL}(P\|Q)=\sum_x P(x)\log\frac{P(x)}{Q(x)}$ | PPO/RLHF       |
| KL  | KL           | $D_{KL}(P\|Q)\neq D_{KL}(Q\|P)$                 | PPO  RLHF      |

 KL ——$D_{KL}(P\|Q)=H(P,Q)-H(P)$—— PPO、RLHF  DPO 。

> ****：[E.4.3 PPO、RLHF  DPO ](./information-rlhf-dpo) ——  KL 。
