\documentclass[border=10pt, tikz]{standalone}
\usepackage{tikz}
\usepackage{fontspec}
\setmainfont{Songti SC}
\setsansfont{Heiti SC}
\usetikzlibrary{arrows.meta, positioning}

\begin{document}
\begin{tikzpicture}[
    box/.style={
        draw=black!70, rounded corners=8pt, minimum width=3.6cm, minimum height=1.7cm,
        font=\normalsize, align=center, line width=1pt, fill=black!3
    },
    arrow/.style={
        -{Stealth[length=7pt, width=5pt]}, line width=1.1pt, black!70,
        shorten >=4pt, shorten <=4pt
    },
    feedback/.style={
        -{Stealth[length=7pt, width=5pt]}, line width=1.1pt, black!55,
        shorten >=4pt, shorten <=4pt
    },
    lbl/.style={font=\small, align=center, text=black!70, fill=white, inner sep=2pt}
]

\node[box] (actor) at (0, 2.4) {演员\\[-2pt]{\small (Actor)}\\[-2pt]{\footnotesize 决定动作 $a$}};

\node[box] (env) at (5.6, 0) {环境\\[-2pt]{\small (Environment)}};

\node[box] (critic) at (0, -2.4) {评委\\[-2pt]{\small (Critic)}\\[-2pt]{\footnotesize 估计价值 $V(s)$}};

\draw[arrow] (actor.east) -- node[above, lbl, pos=0.48] {动作 $a$} (env.north west);

\draw[arrow] (env.west) -- node[above, lbl, pos=0.48] {状态 $s$} (actor.south east);

\draw[arrow] (env.south west) -- node[below, lbl, pos=0.48] {奖励 $r$，下一状态 $s'$} (critic.east);

\draw[feedback] (critic.north) -- node[left, lbl, align=right] {TD 误差\\[-1pt]（打分）} (actor.south);

\end{tikzpicture}
\end{document}
